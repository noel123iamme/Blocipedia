<h1>Edit Wiki</h1>
 
<div class="row">
  <div class="col-md-8">
    <%= render partial: 'form', locals: { wiki: @wiki } %>
  </div>
</div>
<br>
<h3>Manage Collaborators</h3>
<div class='js-collaterators'>
  <%= render partial: 'collaborators/collaborator', collection: @collaborators %>
</div>
<br>
<% if current_user.admin? || (current_user.premium? && @wiki.user_id = current_user.id) %>
  <div class='new-collaborator'>
    <%= render partial: 'collaborators/form', locals: { collaborator: @collaborator } %>
  </div>
<% end %>